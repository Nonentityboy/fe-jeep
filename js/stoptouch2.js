const container_four = document.getElementById('container_four');

container_four.addEventListener('touchstart', function (e) {
  e.preventDefault();
});
container_four.addEventListener('touchend', function (e) {
  e.preventDefault();
});
container_four.addEventListener('touchmove', function (e) {
  e.preventDefault();
});